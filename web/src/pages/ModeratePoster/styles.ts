import styled from "styled-components";
import {cssValues} from "../../components/GlobalStyles";
import ModeratePosterPage from "./index";

export const ModeratePosterPageContainer = styled.div`
    padding: ${cssValues.blocksGap} 0;
`;