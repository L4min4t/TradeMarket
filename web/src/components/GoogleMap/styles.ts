import styled from "styled-components";
import {cssValues} from "../GlobalStyles";

export const CustomMap = styled.iframe`
    width: 100%;
    height: 100%;
    border: none;
    border-radius: ${cssValues.borderRadius};
`;